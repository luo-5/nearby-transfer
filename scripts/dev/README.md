# Local development scripts

Machine-specific deployment, device-control, VM, credential, and release helper
scripts are intentionally not stored in this public directory. Keep local automation
outside the repository and pass configuration through environment variables when a
portable maintainer tool is promoted into the project.

Only reviewed, non-interactive, cross-platform-safe tools that are exercised by a
documented workflow should be added here.
