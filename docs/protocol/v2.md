# Nearby Transfer protocol v2

> Status: **foundation contract**. Version 2 is intentionally incompatible with
> the v0.x single-file protocol. Discovery, pairing, transfer manifests, chunk
> exchange, and file-library sessions will all use this version number.

## Scope and transport

Protocol v2 is a LAN-only application protocol. UDP discovery is only a hint
that a device may be reachable; it is never authorization. Any operation that
reads or writes user files requires a previously paired device identity and an
authenticated session over the selected transport.

All control messages are UTF-8 JSON. All messages include:

```json
{
  "app": "nearby-transfer",
  "protocolVersion": 2,
  "type": "message-type"
}
```

A recipient must reject a message when the application ID, version, type,
field type, bounds, signature, or peer identity does not match its expected
contract. Unknown fields may be ignored only after the signed/authorized
message schema explicitly permits extensions.

## Current desktop LAN pairing bootstrap

The current desktop implementation has a deliberately narrow TCP bootstrap for
pairing only. Signed v2 discovery announcements are multicast every two seconds
on `239.255.77.77:47777`; they advertise the bootstrap TCP port and are
accepted only when their exact canonical JSON representation, Ed25519
signature, identity-derived device ID/fingerprint, port, capability list, and
freshness window are valid. A discovery record is an unauthenticated reachability
hint: it does not create trust, expose a file service, or authorize a transfer.

The TCP bootstrap uses the length-prefixed v2 wire frame. During this phase it
accepts only `pairing-offer`, `pairing-confirm`, and `pairing-cancel`; a
`transfer-manifest`, chunk, library, unknown type, non-canonical control
payload, oversized payload, excessive frame count, or a pairing/identity change
on one connection closes that connection. The server also caps total and
per-source-IP connections and has a short bootstrap timeout.

This bootstrap is **signed but not confidential**. It must not carry file
metadata, file bytes, library data, credentials, or other sensitive application
content. Transfer and library messages remain disabled until a later phase adds
an authenticated ephemeral X25519 key exchange, HKDF-derived directional keys,
AEAD encryption, sequence/replay protection, and explicit authorization against
a trusted peer record.

## Canonical JSON for signatures

Signatures use the restricted canonical JSON format implemented in:

- desktop: `src/v2/canonical-json.js`;
- Android: `ProtocolV2.canonicalJson`.

Rules:

1. Objects use lexicographically sorted Unicode-code-unit keys.
2. Arrays keep their existing order.
3. Only `null`, booleans, strings, arrays, plain objects, and safe integer
   values are allowed. Floats, exponent notation, `undefined`, binary values,
   and non-finite values are invalid.
4. Strings are UTF-8 JSON strings. Unpaired UTF-16 surrogates are invalid.
5. No whitespace is emitted outside JSON strings.

This removes implementation-specific object-property ordering from signed
payloads. The shared fixture is `test/fixtures/protocol-v2-pairing.json`; both
Node and Android JVM tests must pass it unchanged.

## Device identity

Every device has a long-lived Ed25519 signing key and X25519 key-agreement
key. Its public identity has the fields below:

```json
{
  "deviceId": "first 16 lowercase SHA-256 hex characters of signingPublicKey",
  "deviceName": "up to 128 non-blank characters",
  "fingerprint": "six uppercase SHA-256 groups of the signing public key",
  "signingPublicKey": "Ed25519 SPKI PEM",
  "encryptionPublicKey": "X25519 SPKI PEM"
}
```

The receiver recomputes `deviceId` and `fingerprint`, parses both PEM keys, and
rejects a mismatched type or identity. A device identity change invalidates all
trust records and requires a new pairing.

## Pairing

1. The initiator creates a random 16-byte base64url `pairingId` and sends a
   signed `pairing-offer` through discovery or QR bootstrap.
2. The responder validates the offer, replies with its own signed identity, and
   both sides derive the same six-digit comparison code from the complete,
   role-ordered identity transcript.
3. Both users explicitly confirm the displayed code. A mismatch, timeout,
   cancellation, or signature failure rejects the pairing.
4. Only after both confirmations are signed and stored is the peer trusted.

The code transcript is canonical JSON of:

```json
{
  "app": "nearby-transfer",
  "protocolVersion": 2,
  "type": "pairing-code",
  "pairingId": "base64url-encoded 16 bytes",
  "initiator": { "public identity": "..." },
  "responder": { "public identity": "..." }
}
```

The six-digit code is `uint32be(SHA-256("nearby-transfer/v2/pairing-code\\0" || transcript)[0..3]) mod 1,000,000`, zero-padded. It is a short
authentication string: users must compare it on both devices before trusting
the peer. It is not a password and is never stored as a credential.

A `pairing-offer` is canonical-signed with the initiator's Ed25519 key over:

```json
{
  "app": "nearby-transfer",
  "protocolVersion": 2,
  "type": "pairing-offer",
  "pairingId": "...",
  "issuedAt": 1760000000000,
  "identity": { "public identity": "..." },
  "capabilities": ["file-library", "transfer"]
}
```

Capabilities are lower-case ASCII tokens, de-duplicated and sorted before
signing. Desktop pairing sessions are limited to 32 active records per device
and expire after five minutes. An inbound-offer replay may return its existing
active responder session, but a pairing ID may not be reused for a different
role or identity.

After a user has compared and approved the code, its device sends a separate
Ed25519-signed `pairing-confirm` message. The confirmation is bound to the
exact pairing ID, device ID, comparison code, and issue time:

```json
{
  "app": "nearby-transfer",
  "protocolVersion": 2,
  "type": "pairing-confirm",
  "pairingId": "...",
  "issuedAt": 1760000000100,
  "deviceId": "first 16 lowercase SHA-256 hex characters",
  "pairingCode": "042069"
}
```

The receiver verifies the message against the public key in the previously
validated remote offer, rejects a mismatched code or device ID, and permits the
trusted-peer write only after both local and verified remote confirmations are
present. A renderer may request its own confirmation but cannot submit a
remote confirmation directly; that operation remains in the desktop main
process transport boundary.

Either side can terminate an in-progress session with a signed
`pairing-cancel` bound to the pairing ID, sender device ID, reason, and issue
time. Allowed reasons are `connection-closed`, `rejected`, `timeout`, and
`user-cancelled`. Cancellation ends the session and never writes a trusted-peer
record.

## Authenticated message families and availability

The following names are reserved so desktop and Android can evolve without
another incompatible rename:

- `discovery-announce`
- `pairing-offer`, `pairing-confirm`, `pairing-cancel`
- `transfer-manifest`, `transfer-chunk`, `transfer-complete`
- `library-session`

The production desktop and Android composition roots still enable only the
three pairing messages. The Android transport now has an explicit opt-in
transfer-manifest route, but it remains disabled unless a transfer handler is
provided. Library traffic is not routed. Transfer and library traffic must
never be accepted from an unpaired peer.

The transfer foundation now implements the following cross-platform primitives
behind that closed runtime boundary:

- `transfer-manifest`, `transfer-decision`, `transfer-resume`,
  `transfer-progress`, and `transfer-complete` use exact canonical JSON schemas
  and canonical unpadded 64-byte Ed25519 signatures;
- every bootstrap attempt uses a fresh 16-byte canonical base64url `sessionId`;
  the signed manifest and reverse-route decision carry the same value, and every
  signed stream command is bound to it so a valid response from an older
  connection cannot complete a new attempt;
- per-task stream commands use the `transfer-stream-control` envelope, a
  30-second default validity period, exact sender/receiver and direction
  binding, and independent monotonically increasing sequence numbers starting
  at zero in each direction;
- file chunks use ephemeral X25519 agreement, HKDF-SHA256 task binding, and
  AES-256-GCM with authenticated task, path, offset, length, and sequence
  metadata.

The shared fixture `test/fixtures/protocol-v2-transfer-auth.json` requires Node
and Android to produce and verify the same transfer decision and stream-control
bytes. The desktop outgoing bootstrap accepts only a signed reverse-route
decision from the supplied trusted identity for the active task and session,
then requires a signed `transfer-resume` frame for that same session before
handing the socket to the encrypted stream.
The opt-in Android route verifies the manifest
with the trusted sender key, checks the current transfer grant, persists an
`AWAITING_APPROVAL` job, permits one bounded approval request, and detaches the
socket only after a second authorization lookup and an authenticated `accepted`
decision. The sender waits up to 90 seconds by default for the receiver's
60-second approval window. Rejection, timeout, duplicate, malformed, revoked,
changed-key, and runtime-handoff failures close the bootstrap path without
accepting file bytes.

## Resumable transfer checkpoint

The durable checkpoint is receiver-authoritative. The receiver may acknowledge
progress only after decrypting and authenticating a chunk, writing it to
staging, forcing the file, and committing the Room checkpoint. A successful
socket write on the sender is never durable progress.

The persisted checkpoint is independent of a connection and contains:

```json
{
  "files": [
    { "path": "a.txt", "size": 4, "committedOffset": 4, "completed": true }
  ],
  "totalTransferred": 4,
  "nextSequence": 1
}
```

Each connection creates a fresh `sessionId`. `transfer-resume` contains the
complete receiver checkpoint and `transfer-progress` contains the updated
file state after one durable chunk commit. Both messages are signed and bind
the task, session, route, and manifest hash. The persistent checkpoint does not
store `sessionId`, so a retry cannot replay an old connection into a new one.

The current sender uses a bounded kind-3 multiplexed progress frame and waits
for its validated persistence before sending the next chunk. The Android
acknowledgement codec and receiver stream session are unit-tested, while the
production Android runtime composition remains deliberately disabled until
Room checkpoint loading and publication recovery are wired together.

## Migration from v0.x

v0.x discovery and transfer requests use protocol version `1`. v1.0 clients
must advertise and accept version `2` only. Existing local identity material
and device names can be retained, but every existing peer must be paired again.
No downgrade or mixed-version transfer path is provided.

## Transfer manifests

A v2 transfer always starts with a complete `transfer-manifest` before file
bytes are accepted. The manifest is both the durable transfer-job input and the
basis for the receiver's approval UI. It has this exact schema; unknown fields
are rejected.

```json
{
  "app": "nearby-transfer",
  "protocolVersion": 2,
  "type": "transfer-manifest",
  "taskId": "base64url-encoded 16 random bytes",
  "conflictStrategy": "auto-rename",
  "entries": [
    { "kind": "directory", "path": "资料/项目" },
    {
      "kind": "file",
      "path": "资料/项目/说明.txt",
      "size": 12,
      "sha256": "64 lowercase hexadecimal SHA-256 characters"
    }
  ],
  "totalFiles": 1,
  "totalBytes": 12
}
```

`taskId` is exactly 16 random bytes encoded with unpadded base64url. The
conflict strategy is intentionally fixed to `auto-rename`: a receiver must
never overwrite an existing path. The receive implementation chooses a safe,
unique destination name after the user has approved the manifest.

Paths always use relative POSIX separators, even on Windows. A path must be
non-empty; may not be absolute, contain `.` or `..`, backslashes, NUL/control
characters, or Windows-invalid characters; and may not use a Windows reserved
device name (`CON`, `PRN`, `AUX`, `NUL`, `COM1` through `COM9`, or `LPT1`
through `LPT9`, including extensions and Windows-compatible superscript variants). Components may not end in a period or
space. The manifest rejects case-insensitive Windows path collisions and
requires every declared nested directory and every file parent directory to be declared explicitly. Empty
directory declarations are therefore preserved.

Each file declares a non-negative safe-integer byte size (at most 1 TiB) and a
lowercase hexadecimal SHA-256 digest. A manifest contains at most 10,000 total
entries, 8,192 files, and 4 TiB of file data. `totalFiles` and `totalBytes` are
verified computed values, not sender-controlled summaries.

Desktop persistence uses the canonical JSON encoder described above. When a
serialized manifest is read back, it is revalidated and rejected unless its
stored bytes exactly equal the canonical form. This prevents multiple textual
representations of the same durable job and makes subsequent signing/session
work deterministic. The reference desktop module is
`src/v2/transfer-manifest.js`.
