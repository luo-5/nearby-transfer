package io.github.nearbytransfer.android.core.data

/** Storage backend selected for publishing a received transfer. */
enum class PublicationBackend {
    MEDIA_STORE,
    SAF_TREE,
    FILESYSTEM,
}
